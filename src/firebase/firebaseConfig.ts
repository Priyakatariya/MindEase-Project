// firebaseConfig.ts — Placeholder for future Firestore integration
//
// When migrating to Firebase:
// 1. Import Firestore from the existing ../firebase.ts
// 2. Update ../services/storageService.ts to use Firestore collections
//    instead of localStorage
//
// No changes in UI components will be needed — storageService is the
// only abstraction layer that touches persistence.

export { };
